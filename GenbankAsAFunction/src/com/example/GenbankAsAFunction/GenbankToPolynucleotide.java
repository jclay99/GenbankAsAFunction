package com.example.GenbankAsAFunction;

/** This class reads Genbank files and converts them to Polynucleotide
 *  objects, or models describing linear, circular, double stranded, and
 *  single stranded DNA sequences. The Model class is imported from the
 *  biojava library, an openbsource library for DNA sequencing. */

public class GenbankToPolynucleotide {

    /** Instantiates the genbankFile variable; this variable is assigned
     *  in readFile(). */
    private static String genbankFile;

    public static void main(String[] args) {

    }

    /** Assigns the input file path (as a String) to the genbankFile variable.
     *  This variable can be converted to Polynucleotide (Model) in convert(). */
    public static void readFile(String filePath) {

    }

    /** Takes the input genbankFile (assigned in readFile()) and converts
     *  it to a Polynucleotide (Model) file. */
    public static void convert() {

    }

}
